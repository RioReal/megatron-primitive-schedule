{
  "description": "Primary Joint versus one-dimensional structural ablation. The full 520-configuration manifest is generated by scripts/run_primary_ablation.py.",
  "mode": "algorithm-comparison",
  "derive_schedule_only_best": false,
  "repetitions": 1,
  "warmups": 0,
  "parallel_instances": 1,
  "allow_oversubscription": true,
  "timeout_seconds": 600.0,
  "random_seed_base": 1,
  "cp_sat_workers": [16],
  "algorithm_configs": [
    {
      "algorithm": "joint",
      "configuration": "joint"
    },
    {
      "algorithm": "partition-only",
      "configuration": "partition-only"
    },
    {
      "algorithm": "schedule-only",
      "configuration": "schedule-only-uniform",
      "fixed_partition_source": "uniform"
    }
  ]
}
